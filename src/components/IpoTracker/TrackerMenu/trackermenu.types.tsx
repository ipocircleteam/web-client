export type TrackerMenuType = {
  darkMode: boolean
  toggleMode: () => void
}

export type NavButtonsType = {
  toggleFilters: () => void
  toggleFullScreen: () => void
  toggleMode: () => void
  isFullScreen: boolean
  darkMode: boolean
}
