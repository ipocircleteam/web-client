version: '1.0'
services:
  website-client:
    container_name: ipocircle-client
    init: true
    build: 
      context: .
    restart: 'always'
    ports:
      - 3000:3000
    env_file:
      - .env